<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Document</title>
</head>
<body>
   {{#if showTitle}}
   {{{body}}}
   {{/if}}
   {{#if showContent}}
      <form action="/calculator" method="GET">
        <input style="margin-top: 20px;" type="number" checked=""  name="soA" placeholder="So A" required step="0.1" value={{so_a}}>
        <input style="margin-top: 20px;" type="number" name="soB" placeholder="So B" required step="0.1" value={{so_b}}>
        <label for="operator">Phép tính:</label>
        <select name="operator" id="operator">
            <option value="cong">+</option>
            <option value="tru">-</option>
            <option value="nhan">*</option>
            <option value="chia">/</option>
        </select>
        <input class="subscribe-button" type="submit" value="Cal">
      </form>
      {{/if}}
      <h4>{{kq}}</h4>
</body>
</html>